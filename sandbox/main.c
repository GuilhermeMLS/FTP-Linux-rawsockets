#include <stdio.h>
#include <stdlib.h>
#include "/home/leonardo/Documentos/redes1/t1/trab1_redes/messages.h"


void main()
{
    // char *msg = calloc(DATA_SIZE, sizeof(char));
    // srand(2);
    // for (int i = 0; i < TAM_DADOS; i++)
    // {
    //     msg[i] = rand() % 127;
    // }
    // int r = calculateCRC(msg);
    // for (int i = 0; i < TAM_DADOS; i++)
    // {
    //     printf("%X", msg[i]);
    // }
    printf("\n%ld \n", sizeof(message));
}

